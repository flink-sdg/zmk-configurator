export enum ScreenWidth {
    XS = 0,
    SM = 640,
    MD = 768,
    LG = 1024,
    XL = 1280,
    XXL = 1536
};

export enum PositionType {
    FIXED,
    ABSOLUTE
}

export enum Position {
    TOP_LEFT,
    TOP_CENTER,
    TOP_RIGHT,
    RIGHT_CENTER,
    BOTTOM_RIGHT,
    BOTTOM_CENTER,
    BOTTOM_LEFT,
    LEFT_CENTER
}

export enum Alignment {
    RIGHT_BELOW,
    RIGHT_ABOVE,
    LEFT_BELOW,
    LEFT_ABOVE,
    TOP_ARIGHT,
    TOP_ALEFT,
    BOTTOM_ARIGHT,
    BOTTOM_ALEFT
}